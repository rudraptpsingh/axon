Run the full evaluation script and report results.

Run: `python3 scripts/eval_cpu_stress.py ./target/release/axon`

Parse the output and report:
- Which checks passed/failed
- CPU baseline, peak, and recovery values
- Headroom transitions observed
- Any signal quality issues found
- Overall pass/fail verdict
