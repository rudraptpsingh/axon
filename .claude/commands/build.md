Build axon and run all tests. Report any failures.

Run these commands in order:
1. `cargo build --release`
2. `cargo test --workspace`

Report: number of tests passed/failed, any compilation errors.
If all pass, say "[ok] Build clean, N tests passed."
