Take a quick hardware snapshot and interpret it.

Run: `axon query hw_snapshot`

Report:
- CPU usage %
- RAM used/total and pressure level
- Die temperature (if available)
- Throttling state
- Headroom level and reason
- Whether it is safe to run heavy tasks (cargo build, large tests) right now
